document.addEventListener('DOMContentLoaded', async () => {
  const status = document.getElementById('status');
  status.textContent = 'AsynxDL desktop app is running locally. No pairing token needed.';
  status.style.color = '#2e7d32';
});
